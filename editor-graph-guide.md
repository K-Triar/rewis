# Kトライア全世界鉄道情報システム（REWIS） 事業者向けページ操作マニュアル（図形式エディタ）

このマニュアルでは、Kトライア全世界鉄道情報システム（REWIS）の**図形式データ編集ページ（`editor-graph/`）**の基本操作について簡単に説明します。

REWISには、データを表（一覧）の形で編集する**表形式エディタ（`editor.html`）**と、路線図の上で直接クリック・ドラッグしながら編集する**図形式エディタ（`editor-graph/`、このページ）**の2種類があります。どちらも扱うデータは同じなので、途中で切り替えても内容はそのまま引き継がれます。初めて駅や路線・運行系統をまとめて作るときは、図形式のほうが直感的でおすすめです。

---

## 1. はじめに：このページでできること

REWISのデータ編集ページでは、皆さんがアプリなどで見かける「乗換案内」や「運行状況」の元となるデータを作成・管理します。

主に以下のデータを登録・編集することができます。

1. **鉄道会社**・**駅**・**路線** の情報
2. **運行系統** （どの駅にどの順番で停まり、駅間の所要時間はどれくらいかという設定。直通運転もここに含まれます）
3. **のりば乗換・駅グループ** （同じ駅の中で歩いて乗り換える時間や、別々の駅名をまとめて検索できるようにする設定）
4. **運行情報** （遅延や運休のお知らせ）

> [!NOTE]
> ここで入力・保存したデータが、一般の利用者向けの乗換案内ページにそのまま表示されます。

**▼ 図形式データ編集ページへの入り方**

- 公開ページ（トップページなど）の「事業者向けページ」から入った表形式エディタのヘッダー直下にある「**図で編集する（新しいエディタ）**」のリンクから移動してください。
- 直接開く場合のURL：

  [https://k-triar.github.io/rewis/editor-graph/](https://k-triar.github.io/rewis/editor-graph/)

> [!TIP]
> 表形式エディタでログイン済みであれば、図形式エディタを開いたときもログイン済みの状態になります（逆も同じです）。

---

## 2. 画面の見方と基本ルール

まずは、編集画面のどこに何があるかを確認しましょう。

### 2.1 画面の一番上（ヘッダー）

ページの名前と、**「今の接続状態」**（ログイン中のユーザーID、または未ログイン）が表示されます。

### 2.2 その下の行（状態の行）

編集内容がどこまで保存されているかを表す、重要な行です。

| 表示 | 意味 |
|---|---|
| 「路線網 版N」「運行情報 版N」 | 現在読み込んでいるデータの版数です。読み込んでいない間は「未読込」と表示されます。 |
| 「未保存の変更あり」 | まだサーバーに保存していない変更が残っています。 |
| ［エラー N］［注意 N］ | データに問題がある件数です。押すと、エラー・注意の一覧（2.7）が開きます。 |
| ↶／↷（元に戻す・やり直す） | 直前の操作を取り消す／やり直すボタンです。`Ctrl+Z`（元に戻す）／`Ctrl+Y`（やり直す）でも操作できます。 |
| ［↑ サーバーに保存］ | 編集内容を最終的にシステムに反映させるボタンです。未保存の変更がないと押せません。 |
| ［表形式で編集］ | 表形式エディタ（`editor.html`）に切り替えます。 |
| ［？ 操作ガイド］ | 今開いているタブの使い方を、画面に吹き出しで重ねて説明してくれる機能です。駅・路線・運行系統・乗換・駅グループのタブでのみ表示されます。 |

> [!IMPORTANT]
> 図形式エディタでは、入力欄の値を変えたりボタンを押したりすると、**その場ですぐに変更が反映されます**（表形式のように「保存」ボタンで確定する行はありません）。誤って変更した場合は、`Ctrl+Z` で元に戻してください。
> ただし、これはあくまで**画面上（あなたのブラウザの中）だけの変更**です。ほかの人にも見えるようにするには、必ず**状態の行の［サーバーに保存］**を押してください。

### 2.3 タブ

「駅」「路線」「運行系統」「乗換・駅グループ」「運行情報」「鉄道会社」「車両種別」「履歴」「データの読込と書出」の順にタブが並んでいます。

> [!NOTE]
> 表形式のように「左のタブから順番に設定する」という強い制約はありませんが、駅を作ってから路線に加え、路線ができてから運行系統を作る、という流れが基本になる点は表形式と同じです。

### 2.4 駅・路線・運行系統・乗換のタブの共通構成（3列レイアウト）

この4つのタブは、次のような3列の画面になっています。

```
┌ 左：一覧・絞り込み ┬────────── 中央：路線図（キャンバス） ──────────┬ 右：選んだものの設定 ┐
│ 検索や絞り込み       │ 駅・路線・運行系統・乗換を図の上に描画          │ 選択中の内容の編集欄  │
│ 対象の一覧            │ クリック・ドラッグで選択や作成ができる          │                        │
└──────────────────────┴──────────────────────────────────────────────┴────────────────────────┘
```

- **左パネル**：一覧から対象を選ぶと、中央の図でも選ばれた状態になり、右パネルに詳しい設定が出ます。
- **中央の図（キャンバス）**：
  - マウスホイールで拡大・縮小、何もない場所をドラッグすると表示位置を動かせます。
  - 駅の四角（ノード）は、本体をドラッグすると位置を動かせます（乗換タブだけは `Shift` を押しながらドラッグします。詳しくは8章）。動かした配置は駅のデータの一部なので、［サーバーに保存］を押すと**ほかの人にも共有**されます（元に戻す・やり直すも使えます）。
  - 保存するとき、図に出ている駅のうち配置が決まっていないものは、自動配置の位置が保存されます。
- **右パネル**：選んでいるもの（駅・路線・運行系統・停車駅・駅間・乗換など）の詳しい設定欄です。何も選んでいないときは、簡単な操作の説明が出ます。
- 運行系統・路線のタブでは、図の下に**リボン**と呼ぶ横並びの帯が表示され、停車駅や駅順をドラッグで並べ替えたり、`Alt+←` `Alt+→` で動かしたりできます。

### 2.5 操作ガイド

各タブを初めて開いたときは、画面の該当箇所を指し示しながら使い方を説明する「操作ガイド」が自動的に表示されます。2回目以降は出ませんが、状態の行の［？ 操作ガイド］からいつでも再表示できます。

### 2.6 図・一覧の「色」や「印」の意味

入力内容に問題がある場合、次のように色や印でお知らせします。

- **一覧の行に付く「エラー」「注意」の札**：その駅・運行系統などに問題があることを表します。
- **図の中の赤・オレンジの縁取り**：停車駅の番号の丸や、駅間の線がエラー（赤）や注意（オレンジ）の色になります。
- **破線の駅間**：まだ路線・種別が決まっていない区間です（運行系統タブ）。

> [!WARNING]
> エラーが1件でもあると、サーバーに保存できません。注意は保存できますが、内容を確認することをおすすめします。状態の行の［エラー N］［注意 N］から一覧を開き、行をクリックすると該当の場所へ自動的に移動します。

---

## 3. まずはじめに：ログインとデータの読み込み

編集を始める前に、現在システムにある最新のデータを読み込む必要があります。

1. まだネットワークデータを読み込んでいない状態でどのタブを開いても、「はじめに」の画面が表示されます（「データの読込と書出」タブを除く）。
2. **① ログイン**：Workers API URL・ユーザーID・パスワードを入力し、［ログイン］を押します。
   - 表形式エディタで既にログインしている場合は、この手順は不要です（「<ユーザーID> でログイン中」とだけ表示されます）。
3. **② データを読み込む**：［路線網と運行情報を読み込む］を押します。
   - 成功すると、そのまま最後に開いていたタブが表示されます。
   - サーバーにまだデータがない場合や、ログインの期限が切れている場合は、その旨のメッセージが表示されます。

---

## 4. データ編集の基本操作（図形式共通）

### 4.1 対象を選ぶ

- 左パネルの一覧から選ぶか、図の中のノード・線・のりばの点を直接クリックして選びます。

### 4.2 新しく作る

- 駅タブ：図の何もない場所をダブルクリックすると、その位置に新しい駅を作れます。
- 路線・乗換・鉄道会社・駅グループなど：左パネルの［＋◯◯を追加］ボタンから、吹き出しやダイアログで入力して作成します。
- 運行系統：左パネルの［＋運行系統を追加］の後、図の上でのりばを順番にクリックしていくと、その順番で停車する新しい系統ができます（詳しくは7章）。

### 4.3 変更する

- 右パネルの入力欄は、値を変えた瞬間（`change`）にそのまま反映されます。改めて「保存」ボタンを押す必要はありません。
- 数値を入れる欄（所要時間や乗換時間など）に不正な値を入れると、反映されずに元の値へ戻り、赤い枠でエラーが示されます。

### 4.4 削除する

- 右パネルの［◯◯を削除］ボタンを押します。
- そのデータが別の場所（路線・運行系統・乗換など）から使われている場合は削除できず、「使われているため削除できません。」という参照元の一覧が表示されます。まず参照元を直してから、改めて削除してください。
- 削除できる場合は、確認のダイアログが出てから削除されます。

### 4.5 元に戻す

- `Ctrl+Z`（元に戻す）／`Ctrl+Y` または `Ctrl+Shift+Z`（やり直す）で、直前までの操作を100件までさかのぼれます。
- 入力欄にカーソルがある間や、ダイアログを開いている間は効きません。
- 駅の図の中での配置（位置）も、ほかの編集と同じく元に戻す・やり直すの対象です。

---

## 5. 駅タブ

駅の基本情報（ID、名前、ふりがな、のりば）を設定します。

- **左パネル**：駅名・かなで検索できます。まだ図に置かれていない駅には「図にない」の印が付きます。クリックすると、図の中央に自動的に置かれて選ばれます。
- **図**：何もない場所をダブルクリックすると、その位置に新しい駅を作る吹き出しが開きます（駅ID・駅名・かなを入力）。
  - **駅ID**：他の駅と重複しないIDを入力します。**作成した後は変更できません。**
  - **駅名**：*例外事項*：「場所は同じだが会社によって駅名が違う」場合は、**別々の駅として2つ登録**してください。
  - **かな**：カタカナやアルファベットが含まれている駅名でも、検索で見つけやすくするため**すべてひらがな**で入力してください。
- **右パネル**（駅を選択中）：
  - 駅名・かなの変更（駅名を空にはできません）。
  - **のりば**：一覧で名前の変更・並べ替え（つまみをドラッグ）ができます。［のりばを追加］で新しいのりばを作成します（IDは自動候補があります）。
  - 使われているのりば（路線・運行系統・乗換で使用中）は削除できません。参照元の一覧から、該当のタブへ移動できます。
  - **この駅を使っているもの**：路線・運行系統・乗換など、この駅を参照している場所の一覧です。クリックでその場所に移動できます。
  - ［駅を削除］：どこからも使われていない駅だけ削除できます。

> [!WARNING]
> 登録済みの「駅ID」は他の設定でも使われているため、作成後に変更することはできません。
> 全く別の駅で「たまたま名前が同じ」場合は、見分けられるように「駅名（場所）」のように工夫して入力してください。

---

## 6. 路線タブ

路線の名前や色、どの駅に停まるか（駅順序）、路線の形を設定します。

- **左パネル**：路線の一覧（色・形・駅数）と［路線を追加］。
- **図**：選んでいない路線は薄く表示され、クリックするとその路線を選べます。選択中の路線は、駅ノードに駅順の番号が表示されます。
  - 駅間の線をクリックすると、そこに駅を差し込むモードになります。
  - ［駅を末尾に追加］を押した状態で、路線にまだ含まれていない駅をクリックすると、路線の末尾に追加されます。
- **リボン**：駅順が横に並び、ドラッグで並べ替えられます。
- **右パネル**：
  - **路線名・運営会社・路線カラー・車両種別**：会社の一覧から正しい鉄道会社を選びます。
    - *例外事項*：途中で運営会社が変わる路線（現実世界の東海道本線など）は、**会社ごとに分けて別路線として登録**してください。
    - *例外事項*：同じ線路で違う車両種別が走っている場合は、**別路線としてもう一つ登録**してください。
  - **路線の形**：［端あり］［環状線］［ラケット型］の3つから選びます。
    - 環状線にすると、駅順どおり・逆向きの名前を「外回り」「内回り」に変えるか確認されます。
    - ラケット型は、終点から手前のどの駅へ戻るかを図の中でクリックして指定します。
  - **向きの名前**：「駅順どおりに進む向きの名前」「逆向きに進む向きの名前」（例：下り線／上り線、外回り／内回り）。
  - **種別（案内種別）**：急行や各駅停車など、利用者に案内する種別の一覧です。追加・名前の変更・並べ替え・削除ができます。
    - 参照されている種別（運行系統・運行情報で使用中）は削除できません。**1路線につき最低1つは種別が必要です。**
  - 駅を選択中は、［前へ］［後ろへ］で駅順を動かしたり、［路線から外す］で駅を外したりできます。外すことで運行系統がエラーになる場合は、影響する系統名を示す確認が出ます。
  - ［路線を削除］：どこからも使われていない路線だけ削除できます。

---

## 7. 運行系統タブ（いちばん重要なタブ）

「駅から次の駅までの間（区間）」を移動する所要時間や、どのように停車していくかをまとめて設定するタブです。**表形式でいう「区間タブ」と「直通運転タブ」を合わせたものにあたります。** 乗換案内では、同じ条件の列車の隣接する区間同士をつなぎ合わせて、一つの列車として表示しています。

### 7.1 一覧と絞り込み

- 左パネルの上部で、路線・種別による絞り込みができます。
- 一覧の各行には、行先・区間の概要・エラー注意の件数が表示されます。「運行中」がオフの系統には「運休中」の印が付きます。

### 7.2 図とリボンの見方

- 選択していない系統は、駅間ごとに路線の色の細い線で表示されます。クリックするとその系統を選べます。
- 選んでいる系統は、駅間を太い線で結び、色は各区間の路線の色になります。**まだ路線・種別が決まっていない区間は破線**で表示されます。
- 停車駅には番号の丸が付きます。「降車のみ」「乗車のみ」の設定がある場合は、丸の下に小さな札が出ます。
- 別々の路線がつながって走る（直通運転の）境目の駅には、「直通」の印が付きます。**表形式にあった「直通運転タブ」は無く、区間ごとに路線・種別を変えるだけで直通の設定になります。**
- 下のリボンには、停車駅と駅間の所要時間が順番に並びます。ドラッグ、または `Alt+←` `Alt+→` で並べ替えできます。

### 7.3 停車駅・駅間を選んで編集する

- 停車駅の番号の丸、または駅間の線・秒数の札をクリックすると、それぞれ選択され、右パネルに設定欄が出ます。
- **駅間の秒数**（「次の駅まで（秒）」）：停車時間を含めた、次の停車駅に着くまでの秒数です。
  - **TrainCarts**：始点駅で列車が停車した（または出現した）瞬間 から、終点駅で列車が停車した瞬間 までの秒数。
  - **新幹線**：始点駅を通過した瞬間（始発駅はボートを動かし始めた瞬間）から、終点駅を通過した瞬間（終点駅はボートが停止した瞬間）までの秒数。
  - **トロッコ**：始点駅のボタンを押した瞬間（自動発車は列車が動き始めた瞬間）から、終点駅で列車が停車した瞬間 までの秒数。
- **駅間の路線・種別**：範囲選択（`Shift`を押しながらクリック）ができ、［ここから終点まで同じにする］［始発からここまで同じにする］で、まとめて設定できます。
- **停車駅ののりば**：図の中で同じ駅の別ののりばをクリックしても変えられますし、右パネルの選択欄からも変更できます。「この駅から乗れる」「この駅で降りられる」も設定できます（降車専用の駅では乗車のチェックを外します）。
- 停車駅の削除：`Delete` キー、または右パネルの［停車駅を削除］。**停車駅は2つ以上必要です。** 系統ごと消したいときは［運行系統を削除］を使ってください。

### 7.4 停車駅を追加する・新しい運行系統を作る

- ［駅を末尾に追加］を押すと、案内に従って次に停まる駅ののりばを図の中でクリックしていくだけで、末尾に停車駅が加わります。終わったら［完了］か `Esc` を押します。
- 駅間を選んだ状態で［この駅間に駅を追加］を押すと、その間に駅を挿入できます。
- **のりばが登録されていない駅をクリックした場合**は、その場でのりばを追加してから停車させることもできます。
- **新しい運行系統を作る**：左パネルの［＋運行系統を追加］→ 案内に従ってのりばを順にクリックしていきます。2駅目をクリックした時点で一覧に運行系統として現れ、行先が自動的に終点の駅名になり、駅間の秒数・路線・種別も可能な範囲で自動的に入ります。
- **環状運転にする**：停車駅が3つ以上ある状態で、始発駅と同じ駅・同じのりばを再びクリックすると、環状運転にするかどうかの確認が出ます。環状運転では、行先は表示されず、すべての駅間が同じ路線・種別になります。
  - *例外事項（環状運転／回るように運転する路線）*：山手線のように回る路線は、環状運転として登録してください（表形式にあった「今後追加予定」という制限は、図形式では解消されています）。
  - *例外事項（分岐運転）*：利用者の見やすさを考慮して、別の運行系統にするか同じ系統に含めるかを個別に判断してください。

### 7.5 右パネルのそのほかの設定

- **行先**：案内に表示する行先です。環状運転のときは設定できません。［終点の駅名にする］で自動入力できます。
- **メモ（任意）**：編集画面の中だけで使うメモで、利用者には表示されません。
- **運行中**：オフにすると、削除せずに検索・表示から外せます。
- ［複製］：同じ内容の運行系統をもう1つ作ります。
- ［逆方向を作成］：逆向きに走る運行系統を自動的に作ります。作成後は、のりばの設定（片側のみ使うのりばなど）を必ず確認してください。
- ［運行系統を削除］：確認のダイアログの後に削除します。

> [!TIP]
> **【同じ区間で、列車の行先が複数ある場合のルール】**（表形式のときと同じ考え方です）
> - 利用者が「途中駅から行先を選べない」場合：案内上は同じ種別であっても、利用者が迷わないよう「普通（○○行）」「普通（直通）」のように**細かく別の種別として登録**してください。
> - 利用者が「途中駅などで行先を自由に選べる」場合：同じ案内種別としてそのまま登録してください。

---

## 8. 乗換・駅グループタブ

同じ駅の中で乗り換える際に「歩いて何秒かかるか」を設定するほか、別々の駅名を検索上まとめる「駅グループ」を設定します。左パネル上部の切り替えで、「乗換」と「乗換の既定値と駅グループ」を行き来します。

### 8.1 乗換（徒歩連絡）の作成

- 図の中で、のりばの点（または駅の本体）から別の駅ののりば（または本体）へ**ドラッグ**すると、乗換の登録用の吹き出しが開きます。
  - のりばの点から始めるとそののりば指定になり、駅の本体から始めると「のりば指定なし」になります。
- 吹き出しでは、**「乗換時間（秒）」**（「正しい道のりでダッシュせずに歩いたときのリアルな秒数」を入力）、**「逆向きにも同じ時間で乗り換えられる」**（オンにすると往復とも同じ時間になります）、メモを入力し、［乗換を登録］で反映します。

> [!CAUTION]
> **往復で時間が違う乗換は、「逆向きにも同じ時間で乗り換えられる」をオフのままにして、片道ずつ別々に登録してください。**

> [!WARNING]
> **このタブでは、駅の移動は `Shift` を押しながらのドラッグのときだけです。** 普通にドラッグすると乗換の作成になります。

### 8.2 同じ駅の中の乗換（右パネル）

- 駅を選ぶと、のりば同士の乗換時間の表（行＝乗換元、列＝乗換先）が出ます（のりばが2つ以上ある駅だけ）。
- マスに秒数を入れると新規登録、既に登録があれば変更、空にすると削除されます。片方向だけ登録されているマスは斜体で表示され、逆向きの登録を流用していることを示します。

### 8.3 乗換の既定値と駅グループ

- **乗換の既定値**：「同じのりばでの乗換時間（秒）」「のりばがわからない乗換の時間（秒）」を設定します。未設定・同じのりば同士の乗換は、この既定値が自動的に使われます。
- **駅グループ**：「別々の駅名でも、同じ場所として検索の候補にまとめて出すための設定」です。グループ名と、含める駅を指定して作成・編集します。同じ駅を2つのグループに入れることはできません。

---

## 9. 運行情報タブ

列車の遅延、運休、その他のお知らせなど、**利用者に伝える「運行情報」** を作成・公開するための機能です。表形式にあった「運行状況タブ」にあたります。

### 9.1 基本の流れ

- 左パネルで、状態・路線による絞り込みと一覧（既定では下書き・公開中のみ表示）を確認できます。［運行情報を追加］で新規作成します。
- **影響区間の選び方が、図形式ならではの操作です**：対象の路線を選んだ状態で、図の中の駅を2つクリックすると、その間が影響区間として選ばれ、図の上で選択色に強調表示されます。始点と終点に路線の端から端までの駅を選ぶと、自動的に「全線」扱いになります。

### 9.2 設定する項目

- **発生日時**：いつ起きたことかを入力します（下書き以外は月・日が必須です）。
- **影響路線／影響区間**：上記の図での選択に対応します。環状線・ラケット型の路線では、始点から終点へ下り方向に進んだ側が対象になります（反対側にする場合は「始点と終点を入れ替える」を押します）。「全線」も選べます。
- **方向／影響案内種別**：「上りのみ」「急行のみ」など、影響を受けている一部の列車だけを選べます（すべてにチェックも可能）。
- **状態（見合わせ・遅延など）**：テンプレートから選ぶか、「お知らせ」「その他」を選んだ場合は見出し・本文を自分で編集できます。
- **原因（人身事故・車両故障など）**：状態と同様にテンプレートまたは自由入力です。原因が別の場所で起きた場合は、原因路線・原因区間も設定できます。
- **折り返し**：影響区間より手前で一時的に折り返して運転している場合、その駅を指定します。
- **直通設定**：直通運転している路線への影響（「直通運転を中止している」など）がある場合に、候補から選びます。

### 9.3 プレビューと公開設定

- 画面の下部で、入力内容から自動生成された見出し・本文のプレビューを確認できます。
- 自動生成の文章がおかしい場合は「編集」を選び、自分で書き換えられます。
- **「公開済み」にすると、乗換案内ページなどに表示されます**（チェックを外すと非公開の下書きとして残せます）。
- ［複製］［削除］も利用できます。

> [!CAUTION]
> 運行情報タブは、値を変えるとその場で編集中のデータに反映されますが、**利用者に見えるようにするには、状態の行の［サーバーに保存］を押す必要があります。** 保存を忘れると、ブラウザを閉じたときに内容が失われます。

---

## 10. 鉄道会社タブ

鉄道会社のIDと名前を設定します。会社の一覧から選ぶと、名前の変更・削除欄が展開されます。

- **会社ID**は作成後に変更できません。**既に登録されている会社IDは、他のタブで使用されているため絶対に編集しないでください。**
- 路線から参照されている会社は削除できません。
- **自社の会社（`meta.ownCompanyId` に設定されている会社）は削除できません。**

---

## 11. 車両種別タブ

マイクラ（Minecraft）内の鉄道システムの方式（TrainCarts、新幹線、トロッコ）を確認する、**閲覧専用**のタブです。

- **新幹線**：氷ブロックの上を滑走するボートのことです。
- **トロッコ**：マインクラフトの標準のトロッコのことです。

> [!CAUTION]
> **このタブの情報は一切編集できません。** 新しい方式で鉄道を作成した場合は、システムへの追加が必要なため管理者までご連絡ください。

---

## 12. 履歴タブ

過去に「いつ・誰が・どのデータ」を保存したか（更新履歴）を確認するための機能です。「路線網」「運行情報」を切り替えて確認します。

- 一覧から版を選ぶと、右側に詳細が出ます。
- **［今のデータと比べる］**：選んだ版と、今編集中のデータとの件数差を確認できます。
- **［この版を編集中のデータにする］**：選んだ版の内容を、今の編集内容として画面に読み込みます（未保存の状態になります）。
- **［この版に戻す］**：選んだ版の内容を、新しい版としてサーバーに保存し直します。**未保存の変更がある間は使えません。** 先にサーバーに保存するか、変更を取り消してください。

---

## 13. データの読込と書出タブ

これまでの全タブでの編集内容を、**最終的にシステム全体へ反映（保存）させたり、バックアップファイルから読み戻したりする**画面です。ログイン・サーバーからの読み込みも、このタブから行えます。

### 13.1 サーバーから読み込む

路線網・運行情報のそれぞれについて［読み込む］ボタンがあります。未保存の変更がある場合は、失われる旨の確認が出ます。

### 13.2 ファイルへの書き出し・読み込み

- ［ファイルに書き出す］：路線網・運行情報をそれぞれ `network.json`・`operations.json` としてダウンロードします。
- ［ファイルから読み込む］：あらかじめ保存しておいたファイルを読み込みます。ファイルの種類（`kind`）が違う場合や、内容にエラーがある場合は読み込みを中止し、内容を確認できます。件数に差がある場合は、確認のダイアログが出ます。

> [!NOTE]
> v1（旧エディタ）からの移行は、このタブでは行えません。表形式のエディタで行ってください。

---

## 14. 表形式エディタとの使い分け・行き来

- **図形式が向いている場面**：駅や運行系統をまとめて新規作成するとき、路線図の全体像を見ながら直通・乗換の関係を確認したいとき。
- **表形式が向いている場面**：多数のデータを一覧でざっと見直したいとき、細かい項目を一括で修正したいとき。
- **切り替え方**：状態の行の［表形式で編集］を押すと `editor.html` に移動します。未保存の変更がある場合は、「未保存の変更は表形式のエディタに引き継がれません。先にサーバーに保存してください。」という確認が出ます（先にサーバーへ保存してから移動するのが安全です）。
- 表形式エディタのヘッダー直下にある「図で編集する（新しいエディタ）」のリンクから、この図形式エディタに戻れます。

---

このマニュアルに書かれていない操作方法や、うまく動かない点がある場合は、管理者までご連絡ください。
